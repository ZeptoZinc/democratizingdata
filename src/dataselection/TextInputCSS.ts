import { css } from '@emotion/core';

export const textInputCSS = css({
  backgroundColor: '#000000',
  border: 'solid',
  borderWidth: '1px',
  borderColor: '#FFFFFF',
  borderRadius: '0.25rem',
});
